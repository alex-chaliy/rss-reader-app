{
  "name": "@project/rss-reader-app",
  "private": "true",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "typescript": "latest"
  },
  "scripts": {
    "dev": "cd apps/back && npm run dev",
    "install-db": "node setup/install-db-data.js"
  }
}
